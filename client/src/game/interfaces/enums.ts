export enum game {
    MENU,
    GAME
}

export enum typeDialogbox {
    STRINGS,
    BOX,
    CENTER
}

export enum adaptive {
    TWOK,
    FULLHD,
    HD,
    MOBILE
}

export enum load {
    INITIAL,
    LOADING,
    COMPLETE,
    ERROR
}

export enum textSpeedState {
    SLOW,
    MEDIUM,
    FAST,
    MOMENT
}