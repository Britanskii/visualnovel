export enum stateGame {
    MENU,
    SETTINGS,
    GAME
}

export enum typeDialogbox {
    STRINGS,
    BOX,
    CENTER
}

export enum adaptive {
    TWOK,
    FULLHD,
    HD,
    MOBILE
}

export enum stateLoad {
    INITIAL,
    LOADING,
    COMPLETE,
    ERROR
}